# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

####################
# Global variables #
####################

# mod key
set $mod Mod4

# i3-bar
set $bar i3blocks

set $mail thunderbird
set $browser google-chrome-stable
set $music youtube-music

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:FiraCode 10

# key repeating
exec_always --no-startup-id xset r rate 200 40

# Monitor config:
exec_always --no-startup-id ~/.config/i3/monitor-setup.sh

# start redshift, -P to reset
exec_always --no-startup-id "pkill redshift; sleep 1; redshift -x; redshift -P -c ~/.config/redshift/redshift.conf"

# picom
exec_always --no-startup-id picom

# make windows float
for_window [class="floating"] floating enable
for_window [title="mpv webcam"] floating enable

# remove window borders
for_window [class="^.*$"] border none

gaps inner 10
# smart_gaps on
smart_borders on

# restart xautolock with 5 min timer
exec_always --no-startup-id killall xautolock
exec_always --no-startup-id xautolock -time 5 -locker ~/.config/i3/i3lock-color.sh -detectsleep

# start dunst daemon
exec_always --no-startup-id ~/.scripts/dunst-restart.sh

################
# Rofi scripts #
################

# App launcher
bindsym $mod+space exec "rofi -modi drun,run -show drun"

# powermenu
bindsym $mod+p exec --no-startup-id ~/.scripts/rofi/powermenu.sh

# wallpaper
exec_always --no-startup-id ~/.config/i3/load-wp.sh
bindsym $mod+w exec --no-startup-id ~/.scripts/rofi/select-wp.sh

# media menu
bindsym $mod+m exec --no-startup-id ~/.scripts/rofi/musicmenu.sh

# signal i3blocks for changes
set $signal_bar pkill -SIGRTMIN+10 i3blocks

# brightness
bindsym XF86MonBrightnessUp exec xbacklight -inc 5 && $signal_bar
bindsym XF86MonBrightnessDown exec xbacklight -dec 5 && $signal_bar

##########
# Volume #
##########

# init to 0
exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ 0%

# start watcher for i3blocks update
exec_always --no-startup-id ~/.config/i3/volume-watcher.sh

# keybinds
bindsym XF86AudioRaiseVolume exec --no-startup-id ~/.config/i3/bound-volume.sh
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -1%
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle

# pause/play & next/prev
bindsym XF86AudioPlay exec playerctl play-pause && $signal_bar
bindsym XF86AudioNext exec playerctl next && $signal_bar
bindsym XF86AudioPrev exec playerctl previous && $signal_bar

###################
# Caps & Num Lock #
###################

# signal i3blocks for capslock & numlock
bindsym --release Caps_Lock exec --no-startup-id $signal_bar
bindsym --release Num_Lock exec --no-startup-id $signal_bar

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

#################################
# Shortcuts for common programs #
#################################

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# start a floating terminal
bindsym $mod+Shift+Return exec i3-sensible-terminal --class floating

# screenshot selection
bindsym Print exec --no-startup-id ~/.scripts/screenshot.sh -s
# screenshot full window
bindsym Shift+Print exec --no-startup-id ~/.scripts/screenshot.sh -f

bindsym XF86HomePage exec ~/.config/i3/focus-app.sh "google-chrome" $browser
bindsym XF86Mail exec ~/.config/i3/focus-app.sh "Mail" $mail
# bindsym XF86Explorer
# bindsym XF86Calculator
bindsym XF86Tools exec ~/.config/i3/focus-app.sh "com.github.th_ch.youtube_music" $music

# kill focused window
bindsym $mod+Shift+Q kill

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in vertical orientation
bindsym $mod+v split v

# split in horizontal orientation
bindsym $mod+Shift+v split h

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+z layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+g floating toggle

# change focus between tiling / floating windows
bindsym $mod+g focus mode_toggle

# focus the parent container
bindsym $mod+q focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# cycle workspace
bindsym $mod+Tab workspace next

# switch to workspace
bindsym $mod+ampersand workspace number $ws1
bindsym $mod+eacute workspace number $ws2
bindsym $mod+quotedbl workspace number $ws3
bindsym $mod+apostrophe workspace number $ws4
bindsym $mod+parenleft workspace number $ws5
bindsym $mod+minus workspace number $ws6
bindsym $mod+egrave workspace number $ws7
bindsym $mod+underscore workspace number $ws8
bindsym $mod+ccedilla workspace number $ws9
bindsym $mod+agrave workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+eacute move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+egrave move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+ccedilla move container to workspace number $ws9
bindsym $mod+Shift+agrave move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload; exec --no-startup-id $set_monitor
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart; exec --no-startup-id $set_monitor

# resize window (you can also use the mouse for that)
mode "resize" {
    # These bindings trigger as soon as you enter the resize mode

    # Pressing left will shrink the window’s width.
    # Pressing right will grow the window’s width.
    # Pressing up will shrink the window’s height.
    # Pressing down will grow the window’s height.
    bindsym j resize shrink width 10 px or 10 ppt
    bindsym k resize grow height 10 px or 10 ppt
    bindsym l resize shrink height 10 px or 10 ppt
    bindsym m resize grow width 10 px or 10 ppt

    # same bindings, but for the arrow keys
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt

    # back to normal: Enter or Escape or $mod+r
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

# Default colors (fallback if pywal fails)
set $default_background #0A1837
set $default_foreground #f4dc98
set $default_color0     #1c1c1c
set $default_color1     #af5f5f
set $default_color2     #5faf5f
set $default_color3     #d7af5f
set $default_color4     #5f87af
set $default_color5     #af5faf
set $default_color6     #5fafaf
set $default_color7     #d7d7d7
set $default_color8     #3a3a3a
set $default_color9     #ff5f5f
set $default_color10    #87d787
set $default_color11    #ffd787
set $default_color12    #87afd7
set $default_color13    #d787d7
set $default_color14    #87d7d7
set $default_color15    #ffffff


# Source pywal colors
set_from_resource $background background || $default_background
set_from_resource $foreground foreground || $default_foreground
set_from_resource $color0     color0 || $default_color0
set_from_resource $color1     color1 || $default_color1
set_from_resource $color2     color2 || $default_color2
set_from_resource $color3     color3 || $default_color3
set_from_resource $color4     color4 || $default_color4
set_from_resource $color5     color5 || $default_color5
set_from_resource $color6     color6 || $default_color6
set_from_resource $color7     color7 || $default_color7
set_from_resource $color8     color8 || $default_color8
set_from_resource $color9     color9 || $default_color9
set_from_resource $color10    color10 || $default_color10
set_from_resource $color11    color11 || $default_color11
set_from_resource $color12    color12 || $default_color12
set_from_resource $color13    color13 || $default_color13
set_from_resource $color14    color14 || $default_color14
set_from_resource $color15    color15 || $default_color15

set_from_resource $fg i3wm.color7 #f4dc98
set_from_resource $bg i3wm.color3 #0A1837

# client.focused           $bg   $bg   $fg   $bg   $bg
# client.focused_inactive  $bg   $bg   $fg   $bg   $bg
# client.unfocused         $bg   $bg   $fg   $bg   $bg
# client.urgent            $bg   $bg   $fg   $bg   $bg
# client.placeholder       $bg   $bg   $fg   $bg   $bg
# client.background        $bg

# class                 border   backgr. text    indicator child_border
client.focused          $color0  $color1 $color0 $color0 $color0
client.focused_inactive $color0  $color0 $color1 $color0 $color0
client.unfocused        $color0  $color0 $color1 $color0 $color0
client.urgent           $color1  $color0 $color1 $color0 $color0
client.placeholder      $color0  $color0 $color1 $color0 $color0

bar {
    status_command i3blocks
    position top
    colors {
        set_from_resource $fg i3wm.color7 #f4dc98
	    set_from_resource $bg i3wm.color6 #0A1837
        background $color0
        statusline #F9FAF9
        separator  #454947

        # class            border  backgr. text
        focused_workspace  $color0 $color1 $color0
        active_workspace   $color0 $color0 $color1
        inactive_workspace $color0 $color0 $color1
        binding_mode       $color0 $color0 $color1
        urgent_workspace   $color1 $color0 $color1
    }
}
