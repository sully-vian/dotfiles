## st

To install the binary at `~/.local/bin/st`, run:
```bash
make st
```

The config file to edit is at `~/.config/st.h`

## dmenu

To install the binary at `~/.local/bin/dmenu`, run:
```bash
make dmenu
```

The config file to edit is at `~/.config/dmenu.h`
